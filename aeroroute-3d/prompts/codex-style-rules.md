# Prompt: AeroRoute 3D style rules

Style constraints for Codex when building case study UI or copy.

## Portfolio alignment

- Follow existing portfolio style (stone palette, `#f7f6f3` background, `font-display` / `font-mono` patterns).
- Match bilingual patterns used on other `/work/*` pages when ZH copy is added.

## Layout and density

- **Minimal but information-dense** — short labels, scannable cards, clear hierarchy.
- **No long scrolling article style** — prefer slide-like project storytelling.
- Section rhythm similar to flagship case studies (eyebrow → title → body → supporting blocks).

## Product honesty

- Obey `constraints.md` (no real-time flight control, no live thermal claims, synthetic nodes labeled).

## Instructions

<!-- Placeholder — extend when implementing pages -->
